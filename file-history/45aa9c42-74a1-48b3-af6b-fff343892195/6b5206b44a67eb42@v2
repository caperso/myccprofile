import { SeatSelection } from '@ctrip/myaccount-edge/types/postSale';

export const getTotalPrice = (selections: SeatSelection[]): number => {
  let totalPrice = 0;
  selections.forEach(({ seatPrice }) => {
    totalPrice += seatPrice;
  });

  return totalPrice;
};
