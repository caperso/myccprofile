import { SeatSelection } from '@ctrip/myaccount-edge/types/postSale';
import { Seat, SeatRow } from '@xivart/edge/types/seatMap';

import { SeatComponentPassenger } from '~app/domains/PostSale/components/SeatModal/types';

export interface DeckProps {
  combinationId: string;
  currentPassenger: SeatComponentPassenger;
  rows: readonly SeatRow[];
  onClickSeat: (seat: Seat) => void;
  selections: SeatSelection[];
  purchasedSeats: SeatSelection[];
}
